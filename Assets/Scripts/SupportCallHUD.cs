using UnityEngine;

// Compatibility shell for a scene that added the previous HUD component.
// The countdown now lives on JobTicket; this component creates no UI.
[AddComponentMenu("")]
public sealed class SupportCallHUD : MonoBehaviour { }
